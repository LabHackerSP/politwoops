	<h2>
		Políticos que nós seguimos
	</h2>

    <%= render :template => "shared/filterform" %>

<div class="caveat"><p>Essa pode ser uma lista incompleta. Se você acha que estamos esquecendo alguém, por favor <a href="mailto:lab@labhacker.org.br">envie um e-mail</a> informando nome, estado, partido político, cargo que ocupam ou ao qual estão concorrendo e, é claro, o link de seu Twitter. Obrigado!</p>
</div>

	<table class="politicians">
		<tbody>
		<% @politicians.each do |pol|%>
		<tr>
			<td class="img">
        <img src="<%= default_avatar_url(pol) %>"
             class="politician-avatar"
             data-lateimg-src="<%= pol.avatar.url %>"
             alt="<%= pol.user_name %>"
             width"73"
             height="73" />
			</td>
			<td class="name">
				<span>
				<a href="/user/<%= pol.user_name %>/">
					<%= pol.first_name %>
					<%= pol.middle_name %>
					<%= pol.last_name %>
					<%= pol.suffix %>
				</a>
				</span>
			</td>
			<td class="state">
				<span>
				<% if pol.state %> <%= pol.state.upcase %> <% end %>
				</span>
			</td>
			<td class="office">
				<span>
				<% if pol.office %><%= pol.office.title %> <% end %>
				</span>
			</td>

			<!-- 
			<td>
				<%= pol.user_name %>
			</td>
			-->

			<td class="party">
				<span>
				<% if pol.party %> <%= pol.party.display_name %> <% end %>
				<% if pol.status ==  4 %><em>inativo</em> <% end %>
				</span>
			</td>
		</tr>
		<% end %>
		<tbody>
	</table>


        <%= render :template => "shared/pager" %>


